# srelens

> srelens is a Kubernetes desktop workspace — a native GUI app for browsing, inspecting, and operating Kubernetes clusters. It is built on Tauri v2 with a pure-Rust core (kube-rs), renders in the OS system WebView instead of bundled Chromium, and includes a built-in MCP (Model Context Protocol) server that exposes supported backend capabilities as AI-callable tools.

Key facts:
- Product type: desktop application (Kubernetes IDE / cluster workspace)
- Platforms: macOS, Windows, Linux (Tauri v2)
- Status: available — installers published on GitHub Releases for macOS (Apple Silicon + Intel .dmg), Windows (.exe installer, .msi), and Linux (.AppImage, .deb, .rpm); can also be built from source with Rust (stable), Node 22+, and pnpm 9+
- Price: free, open source under the MIT license
- Architecture: Rust core using kube-rs to talk to the Kubernetes API directly — no Electron, no Node.js in the core app runtime
- Lineage: inspired by Lens / Freelens (Electron-based Kubernetes IDEs); srelens is a clean-room rebuild of that workflow, not a fork. Not affiliated with Mirantis (Lens) or the Freelens project.
- All product screenshots on srelens.com are real captures of srelens connected to a live 3-node kind cluster, in both dark and light app themes.

## Pages

- Home: https://srelens.com/
- Features (with real screenshots): https://srelens.com/features/
- MCP server for AI agents: https://srelens.com/mcp/
- Kubernetes tool comparison hub: https://srelens.com/compare/
- srelens vs Lens / K8sLens: https://srelens.com/compare/lens/
- srelens vs Headlamp: https://srelens.com/compare/headlamp/
- srelens vs K9s: https://srelens.com/compare/k9s/
- srelens vs Freelens / OpenLens: https://srelens.com/compare/freelens/
- srelens vs Aptakube: https://srelens.com/compare/aptakube/
- Kubernetes Dashboard alternatives: https://srelens.com/compare/kubernetes-dashboard/
- Download for macOS, Windows, Linux: https://srelens.com/download/
- FAQ: https://srelens.com/faq/

## Features

- Multi-cluster management: every context in your local kubeconfig, cluster hotbar, multiple kubeconfig files, context profiles and custom ordering
- Resource browser covering 40+ Kubernetes kinds: workloads (Pods, Deployments, StatefulSets, DaemonSets, Jobs, CronJobs), config and storage (ConfigMaps, Secrets, PVCs, PVs, StorageClasses), networking (Services, Ingresses, NetworkPolicies, EndpointSlices), RBAC (Roles, ClusterRoles, bindings, ServiceAccounts), admission webhooks, autoscaling, Namespaces, Nodes, Events
- Live streaming resource watches (kube-rs watch streams, not polling)
- Browser-style tabs: each tab is a (cluster, resource-kind) view
- Schema-aware YAML editor with autocomplete and linting driven by the cluster's OpenAPI schema; create resources from templates and apply
- In-pod interactive terminals (exec), docked in the app
- Pod and workload log streaming with container filtering and search
- Port forwarding: start/stop/monitor forwards with a status indicator
- Helm release inspection
- Custom resource (CRD) browsing grouped by API group
- Command palette (Cmd/Ctrl-K): global resource search with deep links
- Cluster metrics: CPU/memory for nodes and pods via metrics-server
- Node and workload operations: scale, rollout restart, evict pod, cordon node, drain node — all destructive actions are confirmation-gated
- Toolbox: manage kubectl, krew, and helm CLI installations from inside the app

## AI / MCP integration

Supported backend capabilities are registered in a shared capability registry and exposed through the built-in MCP server. Agents such as Claude Code, Claude Desktop, Cursor, or other MCP-capable clients connect over stdio (by spawning `srelens --mcp-stdio` via the installed CLI) or loopback HTTP (default `http://127.0.0.1:8765/mcp`). The MCP settings panel generates ready-to-paste client config. Mutating tools require an explicit `_confirm: true` argument before they run. Cluster credentials never leave the machine: srelens connects directly to API servers using the local kubeconfig.

## Common questions

- Does srelens replace kubectl? No — it complements it. It doesn't require kubectl (it uses kube-rs directly) but is a workspace for investigation and operations, not a scripting/automation tool.
- Is it free? Yes, free and MIT-licensed.
- Where does cluster data go? Nowhere — the app runs locally and talks directly to your clusters.

## Links

- Website: https://srelens.com/
- Download: https://srelens.com/download/
- Source / releases: https://github.com/srelens/srelens/releases

## Screenshot inventory (all real captures, dark and light themes)

- Cluster overview: node readiness, pod health, pod distribution, recent warnings — https://srelens.com/assets/shots/dark-overview.webp
- Nodes table with live CPU/memory from metrics-server — https://srelens.com/assets/shots/dark-nodes.webp
- Pods table (live metrics, phases, nodes, and restarts across namespaces) — https://srelens.com/assets/shots/dark-pods.webp
- Pod detail drawer with live metrics — https://srelens.com/assets/shots/dark-pod-detail.webp
- Schema-aware YAML editor — https://srelens.com/assets/shots/dark-yaml.webp
- In-pod exec terminal — https://srelens.com/assets/shots/dark-terminal.webp
- Log streaming dock — https://srelens.com/assets/shots/dark-logs.webp
- MCP settings panel (server, CLI, client config) — https://srelens.com/assets/shots/dark-mcp.webp
- Namespaces with status and resource totals — https://srelens.com/assets/shots/dark-namespaces.webp
- Kubernetes events with reasons, objects, messages, and age — https://srelens.com/assets/shots/dark-events.webp
- Deployments with replicas, images, selectors, and usage — https://srelens.com/assets/shots/dark-deployments.webp
- Services with cluster IPs, ports, selectors, and endpoints — https://srelens.com/assets/shots/dark-services.webp
- Port-forward management — https://srelens.com/assets/shots/dark-port-forwards.webp
- Helm release management — https://srelens.com/assets/shots/dark-helm.webp

Light-theme variants use the same names with the `light-` prefix.

## Demo environment used for screenshots

kind cluster `srelens-demo`: 1 control-plane + 2 worker nodes (Kubernetes v1.35), namespaces `payments`, `checkout`, `monitoring`; workloads include multi-replica Deployments (checkout-api, storefront-web, cart-service, payments-worker, metrics-agent), a redis StatefulSet, a billing CronJob, an HPA, and a deliberately crash-looping `legacy-adapter` Deployment. metrics-server installed for live usage numbers.
